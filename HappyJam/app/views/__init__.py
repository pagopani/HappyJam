
from app.views.homeView import homeView # homeクラスをインポートする
from app.views.instrumentView import instrumentView # instクラスをインポートする
from app.views.genreView import genreView # genruクラスをインポートする
from app.views.videoView import videoView # videoクラスをインポートする
from app.views.camerapreviewView import camerapreviewView # camerapreviewクラスをインポートする
from app.views.continueView import continueView # continueクラスをインポートする
from app.views.editView import editView # editクラスをインポートする
from app.views.previewView import previewView # previewクラスをインポートする